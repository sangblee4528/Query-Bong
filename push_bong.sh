#!/bin/bash

# 1. 작업 디렉토리로 이동 (경로가 다르면 수정하세요)
cd /Users/bong/workspace/Query-Bong

echo "🔍 Git 상태를 확인합니다..."

# 2. 변경사항이 있는지 확인
if [[ -z $(git status -s) ]]; then
    echo "✨ 변경된 내용이 없습니다. 작업이 클린합니다!"
    exit 0
fi

# 3. 동기화 진행
echo "📦 변경사항을 발견했습니다. GitHub로 업로드를 시작합니다."

git add .
git commit -m "Update: 작업 완료 ($(date +'%Y-%m-%d %H:%M:%S'))"
git push origin main

echo "✅ 모든 작업이 GitHub에 안전하게 저장되었습니다!"

